{
  "snapshot": "DEFAULT",
  "install": "set -euo pipefail\nsudo apt-get update -qq\nsudo apt-get install -y -qq python3.12-venv curl >/dev/null\ncd backend\nif [ ! -d .venv ]; then python3 -m venv .venv; fi\n. .venv/bin/activate\npip install -q -r requirements.txt\npip install -q httpx\ncd ../frontend\nif [ -f package-lock.json ]; then npm ci; else npm install; fi",
  "start": "cd backend && . .venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port 8000",
  "ports": [
    {
      "name": "api",
      "port": 8000
    }
  ]
}
