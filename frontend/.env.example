BACKEND_URL=http://localhost:8000
# On Vercel, set BACKEND_URL to your Railway public URL (no trailing slash):
# BACKEND_URL=https://your-service.up.railway.app
#
# NEXT_PUBLIC_API_URL is optional/legacy; the app prefers same-origin /api proxy.